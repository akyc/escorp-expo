{
  "name": "escorp-expo",
  "version": "1.0.0",
  "description": "Exhibition navigator",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel src/*.pug --no-source-maps --open",
    "build": "parcel build src/index.pug src/expo/*.pug --dist-dir build --public-url https://minimir.ru/expo/ --no-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-pug": "^2.7.0",
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.1"
  }
}